import { Router } from 'express';

const router = Router();

// router.post('/users/');
// router.get('/users/:id');
// router.patch('/users/:id');
// router.delete('/users/:id');

export { router as ApiRouter };

